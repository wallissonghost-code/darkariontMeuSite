const BUILD='3.49.0';
const routes={home:{title:'Início',view:'views/home.html',modules:['./home.js','./home-commerce.js']},store:{title:'Mercadorias',view:'views/store.html',modules:['./store-controller.js','./store-runtime-polish.js']},rank:{title:'Rank',view:'views/rank.html',modules:['./rank.js']},card:{title:'Meu cartão',view:'views/card.html',modules:['./cartao.js','./offers.js']},account:{title:'Minha conta',view:'views/account.html',modules:['./perfil.js','./profile-frames.js']}};
const aliases={'home.html':'home','mercadorias.html':'store','rank.html':'rank','cartao.html':'card','ofertas.html':'card','perfil.html':'account'};
const content=document.getElementById('conteudo'),loader=document.getElementById('spaLoader');
const views=new Map(),viewPromises=new Map(),modulePromises=new Map();
let currentRoute='',navigationId=0,loaderTimer=0,roleReady=false,initialViewReady=false,appRevealed=false;
const normalizeRoute=value=>routes[value]?value:'home';
function routeFromUrl(url=new URL(location.href)){const query=url.searchParams.get('page');if(routes[query])return query;return aliases[url.pathname.split('/').pop()]||'home'}
function routeUrl(route){const url=new URL('app.html',location.href);url.searchParams.set('page',route);url.searchParams.delete('_wd');return`${url.pathname}${url.search}`}
function showLoaderDelayed(){clearTimeout(loaderTimer);loaderTimer=setTimeout(()=>{if(loader)loader.hidden=false},100)}
function hideLoader(){clearTimeout(loaderTimer);if(loader)loader.hidden=true}
function updateNavigation(route){document.querySelectorAll('[data-spa-route]').forEach(link=>{const active=link.dataset.spaRoute===route;link.classList.toggle('is-active',active);active?link.setAttribute('aria-current','page'):link.removeAttribute('aria-current')});document.dispatchEvent(new CustomEvent('wd-navigation-ready',{detail:{route}}))}
async function ensureView(route){if(views.has(route))return views.get(route);if(viewPromises.has(route))return viewPromises.get(route);const task=(async()=>{const response=await fetch(`${routes[route].view}?v=${BUILD}`,{cache:'default'});if(!response.ok)throw new Error(`Tela não encontrada (${response.status})`);const section=document.createElement('section');section.className='spa-view';section.dataset.route=route;section.hidden=true;section.style.display='none';section.innerHTML=await response.text();content.append(section);views.set(route,section);return section})().finally(()=>viewPromises.delete(route));viewPromises.set(route,task);return task}
function ensureModules(route){if(modulePromises.has(route))return modulePromises.get(route);const task=(async()=>{await ensureView(route);for(const path of routes[route].modules)await import(`${path}?v=${BUILD}`);views.get(route)?.setAttribute('data-ready','true')})().catch(error=>{modulePromises.delete(route);throw error});modulePromises.set(route,task);return task}
function displayRoute(route){views.forEach((view,key)=>{const visible=key===route;view.hidden=!visible;view.style.display=visible?'block':'none';view.setAttribute('aria-hidden',visible?'false':'true')})}
function revealApp(){if(appRevealed||!roleReady||!initialViewReady)return;appRevealed=true;document.documentElement.dataset.authState='ready';document.body.classList.add('wd-auth-ready');requestAnimationFrame(()=>{hideLoader();document.dispatchEvent(new CustomEvent('wd-app-visible'))})}
function holdSplashUntilPaint(){if(appRevealed)return;document.body.classList.remove('wd-auth-ready');document.documentElement.dataset.authState='loading';if(loader)loader.hidden=false}
document.addEventListener('wd-role-ready',event=>{roleReady=true;holdSplashUntilPaint();revealApp();const name=event.detail?.dados?.nome||event.detail?.user?.displayName||'WD',initials=String(name).trim().split(/\s+/).slice(0,2).map(part=>part[0]||'').join('').toUpperCase()||'WD';document.querySelectorAll('[data-spa-avatar]').forEach(element=>element.textContent=initials)});
function showError(route,error){console.error(`Falha ao abrir ${route}:`,error);document.getElementById('spaError')?.remove();const box=document.createElement('section');box.id='spaError';box.className='spa-error';box.innerHTML='<h2>Não foi possível abrir esta tela</h2><p>Verifique sua conexão e tente novamente.</p><button type="button">Tentar novamente</button>';box.querySelector('button').onclick=()=>{box.remove();navigate(route,{replace:true,force:true})};content.append(box);initialViewReady=true;revealApp()}
async function paintRoute(value,{push=false,replace=false,force=false,boot=false}={}){const route=normalizeRoute(value);if(route===currentRoute&&!force&&views.has(route)&&!document.body.classList.contains('unified-admin-active')){updateNavigation(route);ensureModules(route).catch(error=>console.error('Módulo da tela:',error));return}const id=++navigationId;currentRoute=route;updateNavigation(route);document.body.dataset.spaNavigating='true';if(!boot)showLoaderDelayed();try{await ensureView(route);if(id!==navigationId)return;displayRoute(route);document.getElementById('spaError')?.remove();document.body.classList.remove('unified-admin-active');document.title=`${routes[route].title} — WD Founder`;if(push)history.pushState({route},'',routeUrl(route));else if(replace)history.replaceState({route},'',routeUrl(route));window.scrollTo({top:0,left:0,behavior:'auto'});document.dispatchEvent(new CustomEvent('wd-spa-route',{detail:{route}}));if(boot){initialViewReady=true;revealApp()}else hideLoader();ensureModules(route).catch(error=>console.error(`Falha ao carregar dados de ${route}:`,error))}catch(error){if(id===navigationId)showError(route,error)}finally{if(id===navigationId)delete document.body.dataset.spaNavigating}}
function navigate(value,options={}){return paintRoute(value,options)}
function warmRoute(route){if(route===currentRoute)return;ensureView(route).catch(()=>{})}
document.addEventListener('pointerdown',event=>{const link=event.target.closest('[data-spa-route]');if(!link)return;const route=normalizeRoute(link.dataset.spaRoute);updateNavigation(route);warmRoute(route)},{capture:true,passive:true});
document.addEventListener('click',event=>{const link=event.target.closest('[data-spa-route]');if(link){event.preventDefault();navigate(link.dataset.spaRoute,{push:true});return}const anchor=event.target.closest('a[href]');if(!anchor||anchor.target==='_blank'||event.metaKey||event.ctrlKey||event.shiftKey||event.altKey)return;const url=new URL(anchor.href,location.href);if(url.origin!==location.origin)return;const route=aliases[url.pathname.split('/').pop()];if(route){event.preventDefault();navigate(route,{push:true})}});
window.addEventListener('popstate',event=>navigate(event.state?.route||routeFromUrl()));
const initialRoute=routeFromUrl();if(window.WDSession?.state?.status==='ready')roleReady=true;holdSplashUntilPaint();paintRoute(initialRoute,{replace:true,boot:true});
const warm=()=>Object.keys(routes).filter(route=>route!==currentRoute).forEach(warmRoute);'requestIdleCallback'in window?requestIdleCallback(warm,{timeout:1500}):setTimeout(warm,800);
setTimeout(()=>{if(!appRevealed&&views.has(initialRoute)){initialViewReady=true;if(window.WDSession?.state?.status==='ready')roleReady=true;revealApp()}},2500);
