{
  "name": "darkariont-security-test",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node run-all.mjs",
    "test:static": "node scripts/static-audit.mjs",
    "test:rules": "firebase emulators:exec --only firestore,storage --project demo-darkariont-security \"node --test tests/*.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0"
  }
}
