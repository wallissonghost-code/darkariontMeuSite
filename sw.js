const RELEASE='wd-founder-3.52.0';
const STATIC_CACHE=`${RELEASE}-static`;
const RUNTIME_CACHE=`${RELEASE}-runtime`;
const OFFLINE_FALLBACK='./index.html';
const PRECACHE=['./app.html','./views/home.html','./views/store.html','./splash-lite.css','./mobile-nav-fixed.css','./mobile-nav-account-premium.css','./member-commerce.css','./mobile-pwa.css','./app-updater.js','./app-session.js','./app-shell.js','./spa.js','./store-fast.js','./firebase.js','./manifest.webmanifest','./index.html'];
async function precacheSafely(){const cache=await caches.open(STATIC_CACHE);await Promise.allSettled(PRECACHE.map(async url=>{const response=await fetch(url,{cache:'reload'});if(response.ok)await cache.put(url,response)}))}
self.addEventListener('install',event=>event.waitUntil(precacheSafely().then(()=>self.skipWaiting())));
self.addEventListener('activate',event=>event.waitUntil((async()=>{const keys=await caches.keys();await Promise.all(keys.filter(key=>key.startsWith('wd-founder-')&&!key.startsWith(RELEASE)).map(key=>caches.delete(key)));await self.clients.claim()})()));
async function cacheFirst(request){const cache=await caches.open(RUNTIME_CACHE);const cached=(await cache.match(request))||(await caches.match(request,{ignoreSearch:true}));if(cached){setTimeout(()=>fetch(request,{cache:'no-cache'}).then(response=>{if(response?.ok)return cache.put(request,response.clone())}).catch(()=>{}),0);return cached}try{const response=await fetch(request);if(response?.ok)cache.put(request,response.clone()).catch(()=>{});return response}catch{return request.mode==='navigate'?(await caches.match(OFFLINE_FALLBACK))||Response.error():Response.error()}}
async function networkFresh(request){try{return await fetch(request,{cache:'no-store'})}catch{return(await caches.match(request,{ignoreSearch:true}))||Response.error()}}
self.addEventListener('fetch',event=>{const request=event.request;if(request.method!=='GET')return;const url=new URL(request.url);if(url.origin!==self.location.origin)return;if(/\/(version\.json|sw\.js|app-updater\.js|app-session\.js|spa\.js|store-fast\.js)$/.test(url.pathname)){event.respondWith(networkFresh(request));return}event.respondWith(cacheFirst(request))});
self.addEventListener('message',event=>{if(event.data?.type==='SKIP_WAITING')self.skipWaiting();if(event.data?.type==='CLEAR_OLD_CACHES')event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key.startsWith('wd-founder-')&&!key.startsWith(RELEASE)).map(key=>caches.delete(key)))))});
